<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.michal.konferencje">

    <application
        android:name=".ConferenceApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main_activity.MainActivityView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginactivity.LoginActivityView"
            android:windowSoftInputMode="adjustPan|adjustResize"  />
        <activity android:name=".usercard.UserCardActivityView" />
        <activity android:name=".editprofilactivity.EditProfilActivityView" />
        <activity android:name=".categoryconference.CategoryAndConferenceView"/>
    </application>

</manifest>